name: Build All Packages

on:
  workflow_dispatch:
  push:
    branches: [next]
    paths:
      - 'package-lock.json'
      - 'packages/**'
  pull_request:
    branches: [next]
    paths:
      - 'package-lock.json'
      - 'packages/**'

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-only:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: 'npm'
          cache-dependency-path: 'package-lock.json'

      - name: Validate repository.url in packages
        run: ./.github/scripts/validate-package-repository.sh

      # Runs BEFORE the build on purpose: this failure mode does not show up in a monorepo build,
      # only on a fresh `mj app install`, so the build passing proves nothing about it.
      - name: Validate client/server package boundary
        run: ./.github/scripts/validate-client-package-boundary.sh

      - name: Install dependencies
        run: npm ci

      - name: Build all packages
        run: npm run build:packages
