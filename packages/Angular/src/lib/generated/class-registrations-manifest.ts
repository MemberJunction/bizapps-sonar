/**
 * PLACEHOLDER — This file is owned by `mj codegen manifest`.
 *
 * Once Sonar entities and form components exist, regenerating the manifest
 * will replace this file with named imports of every @RegisterClass
 * decorated class, creating a static code path that prevents tree-shaking
 * from removing them.
 */

/* eslint-disable @typescript-eslint/no-unused-vars */

/**
 * Runtime references to every @RegisterClass decorated class.
 * This array creates a static code path the bundler cannot tree-shake.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export const CLASS_REGISTRATIONS: any[] = [
];

/** Marker constant indicating the manifest has been loaded. */
export const CLASS_REGISTRATIONS_MANIFEST_LOADED = true;

/** Total @RegisterClass decorated classes discovered in dependency tree */
export const CLASS_REGISTRATIONS_COUNT = 0;

/** Packages imported by this manifest */
export const CLASS_REGISTRATIONS_PACKAGES = [
] as const;
