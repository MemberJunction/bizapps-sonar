---
---

Housekeeping: add the dev/test scripts (`scripts/`) and the product/design docs (`plans/`) to the repo. Repo tooling + documentation only — no published-package code changes, so no version bump.
